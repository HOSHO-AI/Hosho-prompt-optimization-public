{
  "name": "hosho-prompt-optimization-public",
  "version": "1.0.0",
  "description": "GitHub Action client for Hosho prompt factor evaluation API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "ncc build build/index.js -o dist --source-map --license licenses.txt",
    "package": "npm run build && npm run bundle",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "github-action",
    "prompt-engineering",
    "ai",
    "code-review"
  ],
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0",
    "diff": "^8.0.3",
    "minimatch": "^10.2.1"
  },
  "devDependencies": {
    "@types/diff": "^7.0.2",
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
